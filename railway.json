{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan key:generate --force && php artisan serve --port=$PORT --host=0.0.0.0",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}